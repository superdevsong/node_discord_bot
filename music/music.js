const execute = require('./execute');
const resume = require('./resume');
const stop = require('./stop');
const skip = require('./skip');

const Music = {
    execute, resume, stop, skip
}

module.exports = Music;
